Validate the current Unity UI skill in a real opencode worker session. This is a bounded read-only capability and image-delivery probe, not an implementation task. You are not alone in the codebase: preserve all existing changes; do not modify repository files, global configuration, install anything, generate images, access credentials, use other providers, or spawn agents.
Read these CURRENT repository files:
$REPO_ROOT/unity-mcp-ui-layout/SKILL.md
$REPO_ROOT/unity-mcp-ui-layout/references/agent-capability-routing.md
$REPO_ROOT/unity-mcp-ui-layout/references/image-asset-workflow.md
Your only input image is $PROBE_ROOT/probe.png. Do not inspect other files in that temporary directory, especially expected.json or peer outputs. Your only writable location is $PROBE_ROOT/opencode/.
First inventory your own session's image-input/vision, image-generation skill (identify any applicable skill actually available/readable to you), generation tool/execution path, filesystem and Unity access. Parent capabilities do not count. No secret contents or full environment dumps.
For the image probe, try your session's normal exposed image read/view path only if available. Do not use OCR, pixel-parsing code, image-to-text proxies, or another agent to substitute for your model seeing the image. If you cannot receive/interpret actual pixels, report unavailable or unknown and do not guess. If you can see it, report the shapes, colors, counts, their rough arrangement, and the printed numeric code. Record the tool or attachment path actually used and distinguish tool capability from execution proof.
Then answer this independent routing case from the skill: a coordinator has an image-generation skill; a child has an image tool but no generation skill and no vision; a separate reviewer can see images. Which exact roles can each take, can the child ask the user about image generation or generate, and what should its completion report say? Do not perform image generation for this test.
Write $PROBE_ROOT/opencode/report.json with executor/model if exposed, capabilities (state/evidence), image_probe (status/observations or limitation), routing_case, and limitations. Write a brief final answer with the report path. Follow your live Orca dispatch preamble for worker_done once finished; unavailable capabilities are a successful diagnostic result, not a task failure.