interface:
  display_name: "Unity MCP UI Layout"
  short_description: "Safer Unity UI layout work through MCP."
  default_prompt: "Use $unity-mcp-ui-layout to build or fix a Unity UI layout through staged edits, layout checks, and screenshot verification."
